// map
const points: number[] = [2, 5, 7, 45, 76, 42, 87]

const maping: number[] = points.map(point => point+10);
// console.log(maping);


// filter
const filtering: number[] = points.filter(point => point > 10);
// console.log(filtering);


// find 
const finding: number[] = points.find(point => point > 20);
// console.log(finding);


